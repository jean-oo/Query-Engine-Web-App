{
	"title": "Where is empty, wrong in OPTION, invalid query",
	"input": {
		"WHERE": {
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_abc",
				"courses_avg"
			]
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
